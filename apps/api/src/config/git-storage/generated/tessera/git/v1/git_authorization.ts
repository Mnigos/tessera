// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: tessera/git/v1/git_authorization.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, Metadata, UntypedServiceImplementation } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export const protobufPackage = "tessera.git.v1";

export interface AuthorizeReadRequest {
  ownerUsername: string;
  repositorySlug: string;
  service: string;
  action: string;
}

export interface AuthorizeReadResponse {
  repositoryId: string;
  storagePath: string;
  trustedUser: string;
}

export interface AuthorizeWriteRequest {
  ownerUsername: string;
  repositorySlug: string;
  service: string;
  action: string;
  basicUsername: string;
  token: string;
}

export interface AuthorizeWriteResponse {
  repositoryId: string;
  storagePath: string;
  trustedUser: string;
}

export const TESSERA_GIT_V1_PACKAGE_NAME = "tessera.git.v1";

function createBaseAuthorizeReadRequest(): AuthorizeReadRequest {
  return { ownerUsername: "", repositorySlug: "", service: "", action: "" };
}

export const AuthorizeReadRequest: MessageFns<AuthorizeReadRequest> = {
  encode(message: AuthorizeReadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ownerUsername !== "") {
      writer.uint32(10).string(message.ownerUsername);
    }
    if (message.repositorySlug !== "") {
      writer.uint32(18).string(message.repositorySlug);
    }
    if (message.service !== "") {
      writer.uint32(26).string(message.service);
    }
    if (message.action !== "") {
      writer.uint32(34).string(message.action);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthorizeReadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthorizeReadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ownerUsername = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.repositorySlug = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.service = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.action = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAuthorizeReadResponse(): AuthorizeReadResponse {
  return { repositoryId: "", storagePath: "", trustedUser: "" };
}

export const AuthorizeReadResponse: MessageFns<AuthorizeReadResponse> = {
  encode(message: AuthorizeReadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repositoryId !== "") {
      writer.uint32(10).string(message.repositoryId);
    }
    if (message.storagePath !== "") {
      writer.uint32(18).string(message.storagePath);
    }
    if (message.trustedUser !== "") {
      writer.uint32(26).string(message.trustedUser);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthorizeReadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthorizeReadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repositoryId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.storagePath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.trustedUser = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAuthorizeWriteRequest(): AuthorizeWriteRequest {
  return { ownerUsername: "", repositorySlug: "", service: "", action: "", basicUsername: "", token: "" };
}

export const AuthorizeWriteRequest: MessageFns<AuthorizeWriteRequest> = {
  encode(message: AuthorizeWriteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ownerUsername !== "") {
      writer.uint32(10).string(message.ownerUsername);
    }
    if (message.repositorySlug !== "") {
      writer.uint32(18).string(message.repositorySlug);
    }
    if (message.service !== "") {
      writer.uint32(26).string(message.service);
    }
    if (message.action !== "") {
      writer.uint32(34).string(message.action);
    }
    if (message.basicUsername !== "") {
      writer.uint32(42).string(message.basicUsername);
    }
    if (message.token !== "") {
      writer.uint32(50).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthorizeWriteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthorizeWriteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ownerUsername = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.repositorySlug = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.service = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.basicUsername = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAuthorizeWriteResponse(): AuthorizeWriteResponse {
  return { repositoryId: "", storagePath: "", trustedUser: "" };
}

export const AuthorizeWriteResponse: MessageFns<AuthorizeWriteResponse> = {
  encode(message: AuthorizeWriteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repositoryId !== "") {
      writer.uint32(10).string(message.repositoryId);
    }
    if (message.storagePath !== "") {
      writer.uint32(18).string(message.storagePath);
    }
    if (message.trustedUser !== "") {
      writer.uint32(26).string(message.trustedUser);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthorizeWriteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthorizeWriteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repositoryId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.storagePath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.trustedUser = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface GitAuthorizationServiceClient {
  authorizeRead(request: AuthorizeReadRequest, metadata?: Metadata): Observable<AuthorizeReadResponse>;

  authorizeWrite(request: AuthorizeWriteRequest, metadata?: Metadata): Observable<AuthorizeWriteResponse>;
}

export interface GitAuthorizationServiceController {
  authorizeRead(
    request: AuthorizeReadRequest,
    metadata?: Metadata,
  ): Promise<AuthorizeReadResponse> | Observable<AuthorizeReadResponse> | AuthorizeReadResponse;

  authorizeWrite(
    request: AuthorizeWriteRequest,
    metadata?: Metadata,
  ): Promise<AuthorizeWriteResponse> | Observable<AuthorizeWriteResponse> | AuthorizeWriteResponse;
}

export function GitAuthorizationServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["authorizeRead", "authorizeWrite"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("GitAuthorizationService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("GitAuthorizationService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const GIT_AUTHORIZATION_SERVICE_NAME = "GitAuthorizationService";

export type GitAuthorizationServiceService = typeof GitAuthorizationServiceService;
export const GitAuthorizationServiceService = {
  authorizeRead: {
    path: "/tessera.git.v1.GitAuthorizationService/AuthorizeRead" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AuthorizeReadRequest): Buffer => Buffer.from(AuthorizeReadRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AuthorizeReadRequest => AuthorizeReadRequest.decode(value),
    responseSerialize: (value: AuthorizeReadResponse): Buffer =>
      Buffer.from(AuthorizeReadResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AuthorizeReadResponse => AuthorizeReadResponse.decode(value),
  },
  authorizeWrite: {
    path: "/tessera.git.v1.GitAuthorizationService/AuthorizeWrite" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AuthorizeWriteRequest): Buffer =>
      Buffer.from(AuthorizeWriteRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AuthorizeWriteRequest => AuthorizeWriteRequest.decode(value),
    responseSerialize: (value: AuthorizeWriteResponse): Buffer =>
      Buffer.from(AuthorizeWriteResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AuthorizeWriteResponse => AuthorizeWriteResponse.decode(value),
  },
} as const;

export interface GitAuthorizationServiceServer extends UntypedServiceImplementation {
  authorizeRead: handleUnaryCall<AuthorizeReadRequest, AuthorizeReadResponse>;
  authorizeWrite: handleUnaryCall<AuthorizeWriteRequest, AuthorizeWriteResponse>;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
